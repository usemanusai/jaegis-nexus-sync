# @nexus-sync/db-schema

Prisma schema and migrations for NexusSync.

- Schema: prisma/schema.prisma
- Usage: Set DATABASE_URL in environment, then run `npx prisma generate` and `npx prisma migrate dev --name init`.

