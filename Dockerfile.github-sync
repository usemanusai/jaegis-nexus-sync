FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* .npmrc* ./
COPY packages ./packages
RUN npm install --no-audit --no-fund && npm run -w packages/github-sync build || true
EXPOSE 33031
CMD ["node", "packages/github-sync/dist/server.js"]

