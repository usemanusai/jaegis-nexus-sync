FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* .npmrc* ./
COPY packages ./packages
RUN npm install --no-audit --no-fund && npm run -w packages/rag-service build
EXPOSE 33001
CMD ["node", "packages/rag-service/dist/main.js"]

